@echo off
REM === Wait a bit so network is ready (optional) ===
timeout /t 10 /nobreak >nul

REM === Go to the folder where this .bat and manage.py live ===
cd /d "%~dp0"

REM === Activate the virtual environment ===
call ".venv\Scripts\activate.bat"

REM === Run Django server on all interfaces ===
python manage.py runserver 0.0.0.0:80
